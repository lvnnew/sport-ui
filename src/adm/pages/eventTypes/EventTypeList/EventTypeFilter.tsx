import React, {FC} from 'react';
import DefaultEventTypeFilter from './DefaultEventTypeFilter';

const EventTypeFilter: FC<any> = (props) => (
  <DefaultEventTypeFilter {...props} />
);

export default EventTypeFilter;
