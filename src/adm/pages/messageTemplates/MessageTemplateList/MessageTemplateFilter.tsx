import React, {FC} from 'react';
import DefaultMessageTemplateFilter from './DefaultMessageTemplateFilter';

const MessageTemplateFilter: FC<any> = (props) => (
  <DefaultMessageTemplateFilter {...props} />
);

export default MessageTemplateFilter;
