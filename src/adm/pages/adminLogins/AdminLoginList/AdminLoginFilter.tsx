import React, {FC} from 'react';
import DefaultAdminLoginFilter from './DefaultAdminLoginFilter';

const AdminLoginFilter: FC<any> = (props) => (
  <DefaultAdminLoginFilter {...props} />
);

export default AdminLoginFilter;
