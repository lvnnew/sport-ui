import React, {FC} from 'react';
import DefaultManagerFilter from './DefaultManagerFilter';

const ManagerFilter: FC<any> = (props) => (
  <DefaultManagerFilter {...props} />
);

export default ManagerFilter;
