import React, {FC} from 'react';
import DefaultMatchStatusFilter from './DefaultMatchStatusFilter';

const MatchStatusFilter: FC<any> = (props) => (
  <DefaultMatchStatusFilter {...props} />
);

export default MatchStatusFilter;
