import React, {FC} from 'react';
import DefaultPlayerMatchRatingFilter from './DefaultPlayerMatchRatingFilter';

const PlayerMatchRatingFilter: FC<any> = (props) => (
  <DefaultPlayerMatchRatingFilter {...props} />
);

export default PlayerMatchRatingFilter;
