import React, {FC} from 'react';
import DefaultWscContactFilter from './DefaultWscContactFilter';

const WscContactFilter: FC<any> = (props) => (
  <DefaultWscContactFilter {...props} />
);

export default WscContactFilter;
