import React, {FC} from 'react';
import DefaultOrganizatorFilter from './DefaultOrganizatorFilter';

const OrganizatorFilter: FC<any> = (props) => (
  <DefaultOrganizatorFilter {...props} />
);

export default OrganizatorFilter;
