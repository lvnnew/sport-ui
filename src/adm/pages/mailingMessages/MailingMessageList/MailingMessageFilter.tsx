import React, {FC} from 'react';
import DefaultMailingMessageFilter from './DefaultMailingMessageFilter';

const MailingMessageFilter: FC<any> = (props) => (
  <DefaultMailingMessageFilter {...props} />
);

export default MailingMessageFilter;
