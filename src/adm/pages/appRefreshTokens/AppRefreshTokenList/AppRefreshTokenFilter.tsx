import React, {FC} from 'react';
import DefaultAppRefreshTokenFilter from './DefaultAppRefreshTokenFilter';

const AppRefreshTokenFilter: FC<any> = (props) => (
  <DefaultAppRefreshTokenFilter {...props} />
);

export default AppRefreshTokenFilter;
