import React, {FC} from 'react';
import DefaultPlayerForMatchRequestFilter from './DefaultPlayerForMatchRequestFilter';

const PlayerForMatchRequestFilter: FC<any> = (props) => (
  <DefaultPlayerForMatchRequestFilter {...props} />
);

export default PlayerForMatchRequestFilter;
