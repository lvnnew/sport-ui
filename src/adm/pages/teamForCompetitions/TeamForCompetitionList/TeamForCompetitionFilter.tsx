import React, {FC} from 'react';
import DefaultTeamForCompetitionFilter from './DefaultTeamForCompetitionFilter';

const TeamForCompetitionFilter: FC<any> = (props) => (
  <DefaultTeamForCompetitionFilter {...props} />
);

export default TeamForCompetitionFilter;
