import React, {FC} from 'react';
import DefaultConfigurationVariableFilter from './DefaultConfigurationVariableFilter';

const ConfigurationVariableFilter: FC<any> = (props) => (
  <DefaultConfigurationVariableFilter {...props} />
);

export default ConfigurationVariableFilter;
