import React, {FC} from 'react';
import DefaultCompetitionFilter from './DefaultCompetitionFilter';

const CompetitionFilter: FC<any> = (props) => (
  <DefaultCompetitionFilter {...props} />
);

export default CompetitionFilter;
