import React, {FC} from 'react';
import DefaultMatchRequestFilter from './DefaultMatchRequestFilter';

const MatchRequestFilter: FC<any> = (props) => (
  <DefaultMatchRequestFilter {...props} />
);

export default MatchRequestFilter;
