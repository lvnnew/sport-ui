import React, {FC} from 'react';
import DefaultEntityFilter from './DefaultEntityFilter';

const EntityFilter: FC<any> = (props) => (
  <DefaultEntityFilter {...props} />
);

export default EntityFilter;
