import React, {FC} from 'react';
import DefaultReportForTeamFilter from './DefaultReportForTeamFilter';

const ReportForTeamFilter: FC<any> = (props) => (
  <DefaultReportForTeamFilter {...props} />
);

export default ReportForTeamFilter;
