import React, {FC} from 'react';
import DefaultPlayerFilter from './DefaultPlayerFilter';

const PlayerFilter: FC<any> = (props) => (
  <DefaultPlayerFilter {...props} />
);

export default PlayerFilter;
