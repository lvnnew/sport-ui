import React, {FC} from 'react';
import DefaultMessageTypeFilter from './DefaultMessageTypeFilter';

const MessageTypeFilter: FC<any> = (props) => (
  <DefaultMessageTypeFilter {...props} />
);

export default MessageTypeFilter;
