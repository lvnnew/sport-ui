import React, {FC} from 'react';
import DefaultAppLoginFilter from './DefaultAppLoginFilter';

const AppLoginFilter: FC<any> = (props) => (
  <DefaultAppLoginFilter {...props} />
);

export default AppLoginFilter;
