import React, {FC} from 'react';
import DefaultParentFilter from './DefaultParentFilter';

const ParentFilter: FC<any> = (props) => (
  <DefaultParentFilter {...props} />
);

export default ParentFilter;
