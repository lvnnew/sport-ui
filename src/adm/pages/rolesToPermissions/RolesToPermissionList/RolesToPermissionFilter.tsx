import React, {FC} from 'react';
import DefaultRolesToPermissionFilter from './DefaultRolesToPermissionFilter';

const RolesToPermissionFilter: FC<any> = (props) => (
  <DefaultRolesToPermissionFilter {...props} />
);

export default RolesToPermissionFilter;
