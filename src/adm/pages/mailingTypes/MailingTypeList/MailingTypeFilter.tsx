import React, {FC} from 'react';
import DefaultMailingTypeFilter from './DefaultMailingTypeFilter';

const MailingTypeFilter: FC<any> = (props) => (
  <DefaultMailingTypeFilter {...props} />
);

export default MailingTypeFilter;
