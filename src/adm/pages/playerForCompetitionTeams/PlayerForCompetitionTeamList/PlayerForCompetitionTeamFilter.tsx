import React, {FC} from 'react';
import DefaultPlayerForCompetitionTeamFilter from './DefaultPlayerForCompetitionTeamFilter';

const PlayerForCompetitionTeamFilter: FC<any> = (props) => (
  <DefaultPlayerForCompetitionTeamFilter {...props} />
);

export default PlayerForCompetitionTeamFilter;
