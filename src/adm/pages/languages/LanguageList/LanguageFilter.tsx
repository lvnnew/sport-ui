import React, {FC} from 'react';
import DefaultLanguageFilter from './DefaultLanguageFilter';

const LanguageFilter: FC<any> = (props) => (
  <DefaultLanguageFilter {...props} />
);

export default LanguageFilter;
