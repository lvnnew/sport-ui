import React, {FC} from 'react';
import DefaultPermissionFilter from './DefaultPermissionFilter';

const PermissionFilter: FC<any> = (props) => (
  <DefaultPermissionFilter {...props} />
);

export default PermissionFilter;
