import React, {FC} from 'react';
import DefaultTemplateStyleFilter from './DefaultTemplateStyleFilter';

const TemplateStyleFilter: FC<any> = (props) => (
  <DefaultTemplateStyleFilter {...props} />
);

export default TemplateStyleFilter;
