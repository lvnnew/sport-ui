import React, {FC} from 'react';
import DefaultManagerLoginFilter from './DefaultManagerLoginFilter';

const ManagerLoginFilter: FC<any> = (props) => (
  <DefaultManagerLoginFilter {...props} />
);

export default ManagerLoginFilter;
