import React, {FC} from 'react';
import DefaultTeamMatchListFilter from './DefaultTeamMatchListFilter';

const TeamMatchListFilter: FC<any> = (props) => (
  <DefaultTeamMatchListFilter {...props} />
);

export default TeamMatchListFilter;
