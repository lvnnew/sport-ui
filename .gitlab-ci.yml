image: node:18

# DO NOT EDIT! THIS IS GENERATED FILE

stages:
  - check
  - previous-image
  - build
  - latest-image
  - deploy
  - deploy-previous

cache:
  paths:
    - .cache
    - .cache_images
    # - node_modules

variables:
  REPO_NAME: $CI_PROJECT_NAME
  VERBOSE: 'true'
  RELEASE: $CI_COMMIT_SHORT_SHA
  KUBECONFIG: /etc/deploy/config
  PROJECT_NAME: mtbase

check:
  stage: check
  image: registry.gitlab.com/making.ventures/images/node-with-tools
  before_script:
    - yarn install --frozen-lockfile
  script:
    - ./check.sh

tag-previous-with-sha:
  extends: .tag-image
  stage: previous-image
  only:
    - master
    - release
  allow_failure: true # Firt run won't be able to create previous image
  variables:
    TAG_ORIGIN: :${CI_COMMIT_REF_SLUG}
    TAG_DESTINATION: :${CI_COMMIT_REF_SLUG}-previous-for-${CI_COMMIT_SHA}

build:
  stage: build
  image:
    name: gcr.io/kaniko-project/executor:debug
    entrypoint: ['']
  before_script:
    - mkdir -p /kaniko/.docker
    - echo "{\"auths\":{\"$CI_REGISTRY\":{\"username\":\"$CI_REGISTRY_USER\",\"password\":\"$CI_REGISTRY_PASSWORD\"}}}" > /kaniko/.docker/config.json
    # - /kaniko/warmer --cache-dir=$CI_PROJECT_DIR/.cache_images --image=browserless/chrome
  script:
    - /kaniko/executor
      --cache-dir=$CI_PROJECT_DIR/.cache_images
      --context ${CI_PROJECT_DIR}
      --dockerfile ${CI_PROJECT_DIR}/Dockerfile
      --destination ${CI_REGISTRY_IMAGE}:${CI_COMMIT_REF_SLUG}
      --destination ${CI_REGISTRY_IMAGE}:${CI_COMMIT_REF_SLUG}-${CI_COMMIT_SHA}
      --single-snapshot
  only:
    - master
    - release

tag-latest:
  extends: .tag-image
  stage: latest-image
  only:
    - master
  variables:
    TAG_ORIGIN: master
    TAG_DESTINATION: latest

.tag-image:
  image:
    name: gcr.io/go-containerregistry/crane:debug
    entrypoint: ['']
  script:
    - crane auth login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
    - crane cp ${CI_REGISTRY_IMAGE}:${TAG_ORIGIN} ${CI_REGISTRY_IMAGE}:${TAG_DESTINATION}
  variables:
    GIT_STRATEGY: none

deploy-dev:
  extends: .deploy-dev
  stage: deploy
  variables:
    ENV: "dev"
    CLUSTER_NAME: "stage01"
    TAG: ":master"
  only:
    - master

deploy-prod:
  extends: .deploy-prod
  stage: deploy
  variables:
    ENV: "prod"
    CLUSTER_NAME: "stage01"
    TAG: ":release"
  only:
    - release
deploy-dev-previous:
  extends: .deploy-dev
  stage: deploy-previous
  when: manual
  variables:
    ENV: "dev"
    CLUSTER_NAME: "stage01"
    TAG: ":${CI_COMMIT_REF_SLUG}-previous-for-${CI_COMMIT_SHA}"
  only:
    - master

deploy-prod-previous:
  extends: .deploy-prod
  stage: deploy-previous
  when: manual
  variables:
    ENV: "prod"
    CLUSTER_NAME: "stage01"
    TAG: ":${CI_COMMIT_REF_SLUG}-previous-for-${CI_COMMIT_SHA}"
  only:
    - release

.deploy-dev:
  extends: .deploy
  stage: deploy
  when: on_success
  only:
    - master
  tags:
    - dev
  variables:
    ENV: "dev"
    DEV: "false"
    HOST: "making.ventures"
    ROOT_ENABLED: "true"
    TAG: ":${CI_COMMIT_REF_SLUG}-${CI_COMMIT_SHA}"
    ADM_JWT_SECRET: ${DEV_ADM_JWT_SECRET}
    ADMIN_RECAPTCHA_REQUIRED_SCORE: ${DEV_ADMIN_RECAPTCHA_REQUIRED_SCORE}
    ADMIN_RECAPTCHA_SECRET_KEY: ${DEV_ADMIN_RECAPTCHA_SECRET_KEY}
    ADMIN_UI_URL: ${DEV_ADMIN_UI_URL}
    APP_ENVIRONMENT: ${DEV_APP_ENVIRONMENT}
    APP_JWT_SECRET: ${DEV_APP_JWT_SECRET}
    APP_NAME: ${DEV_APP_NAME}
    APP_TITLE: ${DEV_APP_TITLE}
    APP_UI_URL: ${DEV_APP_UI_URL}
    AUTHORIZATION_BACKEND_CHECKS_ENABLED: ${DEV_AUTHORIZATION_BACKEND_CHECKS_ENABLED}
    BOOTSTRAP_ENABLED: ${DEV_BOOTSTRAP_ENABLED}
    CUSTOMER_RECAPTCHA_REQUIRED_SCORE: ${DEV_CUSTOMER_RECAPTCHA_REQUIRED_SCORE}
    CUSTOMER_RECAPTCHA_SECRET_KEY: ${DEV_CUSTOMER_RECAPTCHA_SECRET_KEY}
    DATABASE_MAIN_MIGRATION_URI: ${DEV_DATABASE_MAIN_MIGRATION_URI}
    DATABASE_MAIN_READ_ONLY_ENABLED: ${DEV_DATABASE_MAIN_READ_ONLY_ENABLED}
    DATABASE_MAIN_READ_ONLY_URI: ${DEV_DATABASE_MAIN_READ_ONLY_URI}
    DATABASE_MAIN_WRITE_URI: ${DEV_DATABASE_MAIN_WRITE_URI}
    DOCKER_REGISTRY_DOMAIN: ${DEV_DOCKER_REGISTRY_DOMAIN}
    DOCKER_REGISTRY_PASSWORD: ${DEV_DOCKER_REGISTRY_PASSWORD}
    DOCKER_REGISTRY_USERNAME: ${DEV_DOCKER_REGISTRY_USERNAME}
    ES_CLOUD_ID: ${DEV_ES_CLOUD_ID}
    ES_ENABLED: ${DEV_ES_ENABLED}
    ES_NODE: ${DEV_ES_NODE}
    ES_PASSWORD: ${DEV_ES_PASSWORD}
    ES_TLS_REJECT_UNAUTHORIZED: ${DEV_ES_TLS_REJECT_UNAUTHORIZED}
    ES_USERNAME: ${DEV_ES_USERNAME}
    GRAPHQL_PLAYGROUND_ENABLED: ${DEV_GRAPHQL_PLAYGROUND_ENABLED}
    KAFKA_AUTH_ENABLED: ${DEV_KAFKA_AUTH_ENABLED}
    KAFKA_BROKERS: ${DEV_KAFKA_BROKERS}
    KAFKA_ENABLED: ${DEV_KAFKA_ENABLED}
    KAFKA_PASSWORD: ${DEV_KAFKA_PASSWORD}
    KAFKA_QUEUE_ACKS: ${DEV_KAFKA_QUEUE_ACKS}
    KAFKA_QUEUE_DEFAULT_RETRY_TIME: ${DEV_KAFKA_QUEUE_DEFAULT_RETRY_TIME}
    KAFKA_QUEUE_MAX_ATTEMPTS_SIZE: ${DEV_KAFKA_QUEUE_MAX_ATTEMPTS_SIZE}
    KAFKA_QUEUE_STACK_SIZE: ${DEV_KAFKA_QUEUE_STACK_SIZE}
    KAFKA_QUEUE_SUPPORTED_VERSION: ${DEV_KAFKA_QUEUE_SUPPORTED_VERSION}
    KAFKA_QUEUE_WAITING_INTERRUPT_TIME: ${DEV_KAFKA_QUEUE_WAITING_INTERRUPT_TIME}
    KAFKA_SSL_ENABLED: ${DEV_KAFKA_SSL_ENABLED}
    KAFKA_SSL_REJECT_UNAUTHORIZED: ${DEV_KAFKA_SSL_REJECT_UNAUTHORIZED}
    KAFKA_USERNAME: ${DEV_KAFKA_USERNAME}
    LOGS_FORMAT: ${DEV_LOGS_FORMAT}
    LOKI_URL: ${DEV_LOKI_URL}
    S3_ACCESS_KEY_ID: ${DEV_S3_ACCESS_KEY_ID}
    S3_BUCKET_EMAIL_FILES: ${DEV_S3_BUCKET_EMAIL_FILES}
    S3_BUCKET_TMP_FILES_TO_DOWNLOAD: ${DEV_S3_BUCKET_TMP_FILES_TO_DOWNLOAD}
    S3_ENDPOINT: ${DEV_S3_ENDPOINT}
    S3_REGION: ${DEV_S3_REGION}
    S3_SECRET_ACCESS_KEY: ${DEV_S3_SECRET_ACCESS_KEY}
    SMTP_FROM: ${DEV_SMTP_FROM}
    SMTP_HOST: ${DEV_SMTP_HOST}
    SMTP_PASS: ${DEV_SMTP_PASS}
    SMTP_PORT: ${DEV_SMTP_PORT}
    SMTP_USER: ${DEV_SMTP_USER}

.deploy-prod:
  extends: .deploy
  stage: deploy
  when: manual
  only:
    - release
  tags:
    - prod
  variables:
    ENV: "prod"
    DEV: "false"
    HOST: "making.ventures"
    ROOT_ENABLED: "true"
    TAG: ":${CI_COMMIT_REF_SLUG}-${CI_COMMIT_SHA}"
    ADM_JWT_SECRET: ${PROD_ADM_JWT_SECRET}
    ADMIN_RECAPTCHA_REQUIRED_SCORE: ${PROD_ADMIN_RECAPTCHA_REQUIRED_SCORE}
    ADMIN_RECAPTCHA_SECRET_KEY: ${PROD_ADMIN_RECAPTCHA_SECRET_KEY}
    ADMIN_UI_URL: ${PROD_ADMIN_UI_URL}
    APP_ENVIRONMENT: ${PROD_APP_ENVIRONMENT}
    APP_JWT_SECRET: ${PROD_APP_JWT_SECRET}
    APP_NAME: ${PROD_APP_NAME}
    APP_TITLE: ${PROD_APP_TITLE}
    APP_UI_URL: ${PROD_APP_UI_URL}
    AUTHORIZATION_BACKEND_CHECKS_ENABLED: ${PROD_AUTHORIZATION_BACKEND_CHECKS_ENABLED}
    BOOTSTRAP_ENABLED: ${PROD_BOOTSTRAP_ENABLED}
    CUSTOMER_RECAPTCHA_REQUIRED_SCORE: ${PROD_CUSTOMER_RECAPTCHA_REQUIRED_SCORE}
    CUSTOMER_RECAPTCHA_SECRET_KEY: ${PROD_CUSTOMER_RECAPTCHA_SECRET_KEY}
    DATABASE_MAIN_MIGRATION_URI: ${PROD_DATABASE_MAIN_MIGRATION_URI}
    DATABASE_MAIN_READ_ONLY_ENABLED: ${PROD_DATABASE_MAIN_READ_ONLY_ENABLED}
    DATABASE_MAIN_READ_ONLY_URI: ${PROD_DATABASE_MAIN_READ_ONLY_URI}
    DATABASE_MAIN_WRITE_URI: ${PROD_DATABASE_MAIN_WRITE_URI}
    DOCKER_REGISTRY_DOMAIN: ${PROD_DOCKER_REGISTRY_DOMAIN}
    DOCKER_REGISTRY_PASSWORD: ${PROD_DOCKER_REGISTRY_PASSWORD}
    DOCKER_REGISTRY_USERNAME: ${PROD_DOCKER_REGISTRY_USERNAME}
    ES_CLOUD_ID: ${PROD_ES_CLOUD_ID}
    ES_ENABLED: ${PROD_ES_ENABLED}
    ES_NODE: ${PROD_ES_NODE}
    ES_PASSWORD: ${PROD_ES_PASSWORD}
    ES_TLS_REJECT_UNAUTHORIZED: ${PROD_ES_TLS_REJECT_UNAUTHORIZED}
    ES_USERNAME: ${PROD_ES_USERNAME}
    GRAPHQL_PLAYGROUND_ENABLED: ${PROD_GRAPHQL_PLAYGROUND_ENABLED}
    KAFKA_AUTH_ENABLED: ${PROD_KAFKA_AUTH_ENABLED}
    KAFKA_BROKERS: ${PROD_KAFKA_BROKERS}
    KAFKA_ENABLED: ${PROD_KAFKA_ENABLED}
    KAFKA_PASSWORD: ${PROD_KAFKA_PASSWORD}
    KAFKA_QUEUE_ACKS: ${PROD_KAFKA_QUEUE_ACKS}
    KAFKA_QUEUE_DEFAULT_RETRY_TIME: ${PROD_KAFKA_QUEUE_DEFAULT_RETRY_TIME}
    KAFKA_QUEUE_MAX_ATTEMPTS_SIZE: ${PROD_KAFKA_QUEUE_MAX_ATTEMPTS_SIZE}
    KAFKA_QUEUE_STACK_SIZE: ${PROD_KAFKA_QUEUE_STACK_SIZE}
    KAFKA_QUEUE_SUPPORTED_VERSION: ${PROD_KAFKA_QUEUE_SUPPORTED_VERSION}
    KAFKA_QUEUE_WAITING_INTERRUPT_TIME: ${PROD_KAFKA_QUEUE_WAITING_INTERRUPT_TIME}
    KAFKA_SSL_ENABLED: ${PROD_KAFKA_SSL_ENABLED}
    KAFKA_SSL_REJECT_UNAUTHORIZED: ${PROD_KAFKA_SSL_REJECT_UNAUTHORIZED}
    KAFKA_USERNAME: ${PROD_KAFKA_USERNAME}
    LOGS_FORMAT: ${PROD_LOGS_FORMAT}
    LOKI_URL: ${PROD_LOKI_URL}
    S3_ACCESS_KEY_ID: ${PROD_S3_ACCESS_KEY_ID}
    S3_BUCKET_EMAIL_FILES: ${PROD_S3_BUCKET_EMAIL_FILES}
    S3_BUCKET_TMP_FILES_TO_DOWNLOAD: ${PROD_S3_BUCKET_TMP_FILES_TO_DOWNLOAD}
    S3_ENDPOINT: ${PROD_S3_ENDPOINT}
    S3_REGION: ${PROD_S3_REGION}
    S3_SECRET_ACCESS_KEY: ${PROD_S3_SECRET_ACCESS_KEY}
    SMTP_FROM: ${PROD_SMTP_FROM}
    SMTP_HOST: ${PROD_SMTP_HOST}
    SMTP_PASS: ${PROD_SMTP_PASS}
    SMTP_PORT: ${PROD_SMTP_PORT}
    SMTP_USER: ${PROD_SMTP_USER}

.deploy-ui:
  variables:
    DEPLOY_KIND: "ui"

.deploy:
  image:
    name: alpine/helm:3.9.0
    entrypoint: [""]
  before_script:
    - mkdir -p /etc/deploy
    - echo ${KUBE_CONFIG} | base64 -d > ${KUBECONFIG}
  script:
    - NAMESPACE=${NAMESPACE:-"mtbase-${ENV}"}
    - echo NAMESPACE ${NAMESPACE}
    - echo chart ${NAMESPACE}-${DEPLOY_KIND}

    - helm upgrade
      --install
      --wait ${NAMESPACE}-${DEPLOY_KIND} chart
      --timeout 3600s
      -f chart/values_${ENV}.yaml
      --set "global.projectName=${PROJECT_NAME}"
      --set "global.clusterName=${CLUSTER_NAME}"
      --set "global.env=${ENV}"
      --set "global.deployKind=${DEPLOY_KIND}"
      --set "random=:$(date)"
      --set "app.tag=${TAG}"
      --set "ingress.host=${HOST}"
      --set "style=${STYLE}"
      --set "ingress.rootEnabled=${ROOT_ENABLED}"
      --set "admin.recaptcha.publicKey=${ADMIN_RECAPTCHA_PUBLIC_KEY}"
      --set "dockerRegistry.domain=${DOCKER_REGISTRY_DOMAIN}"
      --set "dockerRegistry.password=${DOCKER_REGISTRY_PASSWORD}"
      --set "dockerRegistry.username=${DOCKER_REGISTRY_USERNAME}"
      --namespace ${NAMESPACE}
