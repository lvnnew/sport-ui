
# DO NOT EDIT! THIS IS GENERATED FILE
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
 name: agr-ui
 labels:
    app: agr-back
    chart: "{{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
 annotations:
 {{- range $key, $value := .Values.ingress.annotations }}
   {{ $key }}: {{ $value | quote }}
 {{- end }}
 {{ if .Values.ingress.letsencryptCert }}
   cert-manager.io/cluster-issuer: "letsencrypt-prod"
 {{ end }}
spec:
  rules:
    {{ if .Values.ingress.rootEnabled }}
    - host: {{ .Values.ingress.domains.app }}.apps.{{ .Values.ingress.host }}
      http:
        paths:
          - backend:
              serviceName: agr-ui
              servicePort: 80
    {{ end }}
    - host: {{ .Values.ingress.domains.app }}.{{ .Values.global.env }}.apps.{{ .Values.global.envName }}.{{ .Values.ingress.host }}
      http:
        paths:
          - backend:
              serviceName: agr-ui
              servicePort: 80
  tls:
  - hosts:
    - {{ .Values.ingress.domains.app }}.{{ .Values.global.env }}.apps.{{ .Values.global.envName }}.{{ .Values.ingress.host }}
    secretName: {{ .Values.ingress.domains.app }}.{{ .Values.global.env }}.apps.{{ .Values.global.envName }}.{{ .Values.ingress.host }}-tls
