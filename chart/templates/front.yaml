
# DO NOT EDIT! THIS IS GENERATED FILE
apiVersion: v1
kind: Service
metadata:
  name: med-ui
  labels:
    app: med-ui
spec:
  ports:
  - port: 80
  selector:
    app: med-ui
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: med-ui
spec:
  replicas: {{ .Values.app.replicas }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 3
  selector:
    matchLabels:
      app: med-ui
  template:
    metadata:
      labels:
        app: med-ui
    spec:
    {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
    {{- end }}
      containers:
      - name: med-ui
        image: {{ .Values.dockerRegistry }}/med-ui{{ .Values.app.tag }}
        ports:
        - name: main-port
          containerPort: 80
        imagePullPolicy: Always
        env:
        - name: RANDOM
          value: {{ .Values.random | quote }}
        - name: APP_NAME
          value: med-ui
        - name: ENV
          value: '{{ .Values.global.env }}'
        - name: ENDPOINT
          value: 'https://{{ .Values.ingress.domains.endpoint }}.{{ .Values.global.env }}.apps.{{ .Values.global.envName }}.{{ .Values.ingress.host }}/adm'

        livenessProbe:
          httpGet:
            path: /
            port: main-port
          initialDelaySeconds: 60
          periodSeconds: 5
        readinessProbe:
          httpGet:
            path: /
            port: main-port
          initialDelaySeconds: 60
          periodSeconds: 5
        startupProbe:
          httpGet:
            path: /
            port: main-port
          initialDelaySeconds: 60
          failureThreshold: 10
          periodSeconds: 10

        resources:
          requests:
            memory: "256Mi"
            cpu: "0.15"
          limits:
            memory: "512Mi"
            cpu: "1"
---
